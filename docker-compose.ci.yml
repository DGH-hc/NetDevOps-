version: "3.9"

services:
  # ----------------------------------------
  # PostgreSQL (CI Database)
  # ----------------------------------------
  db:
    image: postgres:16
    container_name: ci_postgres
    environment:
      POSTGRES_USER: test_user
      POSTGRES_PASSWORD: test_pass
      POSTGRES_DB: test_db
    # ❌ NO PORTS IN CI
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U test_user"]
      interval: 5s
      timeout: 3s
      retries: 10

  # ----------------------------------------
  # Redis (Celery Broker/Backend)
  # ----------------------------------------
  redis:
    image: redis:7
    container_name: ci_redis
    # ❌ NO PORTS IN CI
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 3s
      timeout: 3s
      retries: 10

  # ----------------------------------------
  # Mock Device (Simulated REST Device)
  # ----------------------------------------
  mock-device:
    image: ghcr.io/dgh-hc/mockdevice:latest
    container_name: ci_mock_device
    # ❌ NO PORTS IN CI
    healthcheck:
      test: ["CMD", "curl", "-f", "http://mock-device:5001/health"]
      interval: 5s
      timeout: 3s
      retries: 10

  # ----------------------------------------
  # App (FastAPI) — internal service only
  # ----------------------------------------
  app:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        ROLE: app
    container_name: ci_app
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql+psycopg2://test_user:test_pass@db:5432/test_db
      REDIS_URL: redis://redis:6379/0
    healthcheck:
      # Internal check — does NOT need host ports
      test: ["CMD-SHELL", "curl -f http://localhost:8000/health || exit 1"]
      interval: 5s
      timeout: 3s
      retries: 10

  # ----------------------------------------
  # Worker (Celery)
  # ----------------------------------------
  worker:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        ROLE: worker
    container_name: ci_worker
    depends_on:
      app:
        condition: service_started
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql+psycopg2://test_user:test_pass@db:5432/test_db
      REDIS_URL: redis://redis:6379/0

networks:
  default:
    name: ci_network
